spring:
  security:
    user:
      name: user
      password: password
  application:
    name: website
  datasource:
    url: jdbc:postgresql://10.130.15.175:5432/website_db?useSSL=false
    username: postgres
    password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create

server:
  port: 8080
